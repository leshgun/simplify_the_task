name: Flutter

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "master" ]
    paths-ignore:
       - "docs/**"
       - ".vscode/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - uses: dart-lang/setup-dart@9a04e6d73cca37bd455e0608d7e5092f881fd603
      
      - name: Flutter action
        uses: subosito/flutter-action@v2.10.0
        with:
          channel: 'stable'
      
      - name: Install dependencies
        run: flutter pub get

      - name: Verify formatting
        run: dart format --output=none --set-exit-if-changed .

      - name: Analyze project source
        run: flutter analyze

      - name: Run tests
        run: flutter test

      - name: Create file
        run: cat /home/runner/work/simplify_the_task/simplify_the_task/app/google-services.json | base64

      - name: Run build dev
        run: flutter build apk --flavor dev
